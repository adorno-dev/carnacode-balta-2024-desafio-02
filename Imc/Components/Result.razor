@if (RecordResult is not null)
{
    <EditForm Enhance="true" Model="Patient" OnValidSubmit="Submit">
        <img src="assets/icon-no-bg.png" alt="" @onclick="Clear">
        <h2>@RecordResult.Value.Emoji  @RecordResult.Value.Title</h2>
        <p class="hint">@RecordResult.Value.Description</p>
        <button type="submit">Registrar Medidas</button>
        <button @onclick="History">Ver meu histórico</button>
    </EditForm>
}